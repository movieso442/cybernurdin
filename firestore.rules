rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary test rules for the current browser-only prototype.
    // Replace before production with Firebase Auth or server-side Admin SDK writes.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
